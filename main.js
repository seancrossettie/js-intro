// console.log("Connected");

  // function isThisTrue(arg1, arg2) {
  //   if (arg1 !== 'nice' && arg2 !== 'Nice') {
  //     console.log(`You are ${arg}`);
  //   } else if (arg2.length < 8) {
  //     console.log('false')
  //   }
  // }

// isThisTrue('nice', 'Nice');

const canDrive = (age) => {
  if (age >= 15) {
    return true;
  }
}

console.log(canDrive(15));
